<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Para Mi Chelita Preciosa 💖</title>
    <!-- Carga de Tailwind CSS para un diseño responsivo y moderno -->
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;700&family=Playfair+Display:ital,wght@1,700&display=swap" rel="stylesheet">
    <style>
        /* Estilos CSS personalizados */
        body {
            font-family: 'Inter', sans-serif;
            background-color: #ffebee; /* Fondo Rosa muy suave */
        }
        
        .card-container {
            width: 90vw;
            max-width: 450px;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.15);
            border-radius: 16px;
            overflow: hidden;
            transition: all 0.7s ease-in-out;
        }

        /* Estilo de la carta (contenido del poema) */
        .poem-content {
            background-color: #ffffff; /* Color de papel */
            padding: 24px;
            max-height: 0; /* Inicialmente oculto */
            opacity: 0;
            transition: max-height 1.2s ease-in-out, padding 0.5s, opacity 0.8s;
            overflow: hidden;
            /* Estilo de la fuente para el poema */
            font-family: 'Playfair Display', serif;
            color: #333;
            text-align: left;
            line-height: 1.7;
        }

        /* Estado 'open' */
        .card-container.open .poem-content {
            max-height: 1000px; /* Suficientemente grande para el contenido */
            padding: 32px;
            opacity: 1;
        }

        /* Botón de apertura con estilo */
        .open-button {
            padding: 1rem 2rem;
            background-color: #d81b60; /* Rosa intenso */
            color: white;
            border: none;
            border-radius: 9999px; /* Botón redondo */
            box-shadow: 0 4px 15px rgba(216, 27, 96, 0.5);
            transition: transform 0.2s, background-color 0.2s, box-shadow 0.2s;
        }

        .open-button:hover, .open-button:focus {
            background-color: #c2185b;
            transform: scale(1.05);
            box-shadow: 0 6px 20px rgba(216, 27, 96, 0.7);
        }

        .poem-text p {
            margin-bottom: 1.5em;
            text-indent: 1em;
        }
    </style>
</head>
<body class="flex flex-col items-center justify-center min-h-screen p-4">

    <!-- Tarjeta / Contenedor principal -->
    <div id="card-container" class="card-container bg-pink-500">
        
        <!-- Vista Previa / Tapa de la Carta -->
        <div id="cover" class="p-6 bg-pink-600 text-white flex flex-col items-center justify-center text-center">
            <p class="text-3xl font-bold mb-2">Mi Chelita Preciosa 💛</p>
            <p class="text-xl italic font-light">Tienes una carta de Aniversario</p>
        </div>

        <!-- Contenido de la Carta (Inicialmente oculto) -->
        <div id="letter" class="poem-content">
            <div class="text-center border-b-2 border-pink-100 pb-2 mb-4">
                <p class="text-xl font-bold text-pink-700">Feliz Aniversario, Mi Chelita Divina 💖</p>
            </div>
            <div class="poem-text text-base">
                <p>
                    Hola, buenos días mi vida, ¿cómo amaneciste, mi chelita preciosa? 💛
                </p>
                <p>
                    Amor, feliz aniversario, mi princesa hermosa. Te amo infinitamente con todo mi corazón. Sé que te lo he dicho muchas veces, pero desde que llegaste a mi vida, soy la persona más feliz del mundo.
                </p>
                <p>
                    Te has convertido en mi motivación, en mi impulso para seguir adelante cada día. No tenés idea de lo hermosa que sos ante mis ojos. Sin duda alguna, siempre te llevo en mi corazón; ¿cómo no hacerlo, si sos lo más preciado que tengo?
                </p>
                <p>
                    Te amo y te voy a amar por siempre, porque lo que siento por vos no es algo pasajero: es algo que va a vivir en mi corazón por el resto de mi vida.
                </p>
                <p class="text-right text-pink-600 font-bold mt-6 text-lg">
                    — Feliz aniversario, mi chelita divina 💖
                </p>
            </div>
        </div>
    </div>
    
    <!-- Botón para revelar la carta -->
    <button id="open-button" class="open-button mt-10 text-xl font-semibold">
        Abrir la Carta
    </button>

    <!-- Script para manejar la animación de apertura -->
    <script>
        const cardContainer = document.getElementById('card-container');
        const openButton = document.getElementById('open-button');
        const cover = document.getElementById('cover');
        let isOpen = false;

        // Función para cambiar el estado de la carta
        function toggleCard() {
            if (!isOpen) {
                cardContainer.classList.add('open');
                openButton.textContent = "¡Te Amo!";
                // Ocultar la portada después de la transición
                setTimeout(() => {
                    cover.style.display = 'none';
                    // Scroll suavemente al centro de la tarjeta si es necesario
                    cardContainer.scrollIntoView({ behavior: 'smooth', block: 'center' });
                }, 1000); // Espera 1 segundo para que termine la animación
                isOpen = true;
            } else {
                // Si ya está abierta, podemos hacer que el botón cambie de color o haga otra cosa
                openButton.classList.add('animate-ping');
                setTimeout(() => openButton.classList.remove('animate-ping'), 500);
            }
        }

        // Se agrega el evento de clic (funciona para mouse y toque)
        openButton.addEventListener('click', toggleCard);
    </script>
</body>
</html>
